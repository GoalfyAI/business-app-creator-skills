# 常见问题

### 1. 场景包和普通的 Agent 提示词有什么区别？

提示词只约束 Agent 怎么说话。场景包是一整套可执行能力：包含工具集、任务点、编排路径、
验收标准和业务界面，同类任务交给 Agent 就能按固定路径跑完，不用每次重新描述。

### 2. 这个 Skill 会替我执行业务任务吗？

不会。它只负责**制作和优化场景包**。场景包做好之后，具体业务任务由场景包本身在 GoalfyMax
上执行。如果你想直接完成一次性任务，不需要用这个 Skill。

### 3. 一定要用 Workflow 吗？

不一定。Workflow 适合路径固定、步骤明确的流程；路径需要随情况变化时，普通任务点组合反而
更合适。Skill 里有明确的评估流程，Agent 会先判断再决定，不会默认给你做 Workflow。

### 4. 业务界面是必须的吗？

不是。Workflow 是做业务界面的**前提条件，不是理由**——没有 Workflow 一定不能做业务界面，
有 Workflow 也不一定要做。取决于是否真的有业务用户需要一个操作入口。

### 5. 制作过程中的工单是干什么的？

工单只用于记录与审计，不绑定任何场景包、项目或数据集，也不授予任何资产权限。它让整个制作
过程可追溯：做了哪些决策、验证了什么、还剩什么问题。

### 6. 只读诊断和写入修复有什么区别？

只读工单（`mode="read"`）用于诊断，不允许执行任何写操作。确认要改时，需要新建写工单并
关联原诊断工单。这样诊断结论和修改动作在审计上是分开的。

### 7. 为什么我的 Skill 被提示版本过期？

服务端会校验上报的 Skill 版本。版本过低时写操作会被拒绝，需要先升级插件、读取新的
`[skill-version:...]` 标记后重试。详见各平台的 `UPDATE.md`。

### 8. 升级后为什么还要重启客户端？

版本闸门只校验版本串，所以升级后读到新标记就能在当前会话内继续工作。但 Agent 上下文里
加载的 Skill 内容仍是旧版，需要重启（或 Claude Code 的 `/reload-plugins`）才真正生效。

### 9. 我可以手工编辑 `claude-code/` 或 `codex/` 目录吗？

平台安装文档（`README.md`、`AGENTS.md`、`UPDATE.md`、`.mcp.json`）可以直接编辑。
但 `skills/` 子目录是发布流程从 `skill/` 复制的，手工修改会被下次发布覆盖，
而且会导致校验失败——要改 Skill 内容请改 `skill/` 下的唯一源。

### 10. 支持哪些客户端？

目前支持 Claude Code 和 Codex，两者都通过各自的插件管理器安装和升级。

### 11. 需要什么权限才能用？

需要一个 GoalfyMax 个人 API 密钥。你能操作哪些场景包和资产，取决于该账号在 GoalfyMax
上的权限，Skill 本身不放大任何权限。

### 12. 制作过程会产生费用吗？

Skill 和 MCP 工具本身不额外计费。但真机验证会实际运行项目，涉及模型调用、工具调用等，
按 GoalfyMax 的计费规则计算。Skill 会在执行真机验证前先向你确认。
