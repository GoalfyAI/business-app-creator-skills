# 协议四：状态恢复、知识归属、模板选用与平台适配

工单是唯一能跨轮次续作的载体。知识分两类，问题分两类，分开存放。不内置业务模板。平台对象与执行形态的正本在 references。

## 1. 工单记账与续作

`task_manager` 提供五个 action：`create`、`get`、`list`、`insert`、`complete`。

**Gate。** 新的制作或优化任务：`create(mode="write")`，传 SKILL.md frontmatter 里 `[skill-version:...]` 的完整值。只读诊断：`create(mode="read")`。已知 `task_id` 的续作：`get`。不知道 `task_id`：先 `list`，唯一匹配则 `get`，无匹配才 `create`，多个候选交开发者选。只读转修复：新建 `write` 工单并传 `continued_from_task_id`。`read` 工单**不得**写。Gate 失败按 `reason` 处置：`SCENE_SKILL_UPGRADE_REQUIRED`，本会话升级后用已安装 SKILL.md 的版本串重试；`SCENE_SKILL_UPGRADE_RECOMMENDED`，不阻断；`WORKFLOW_TASK_MODE_MISMATCH`，按 `actual_mode` 与 `required_mode` 纠正。**禁止**新建平行工单或改名绕过。细则见 P8 工单 Gate 一节。

**失忆测试。** 写入前问一句：下一轮只剩这份工单，还能不能接着干。不能就必须写；能就**禁止**写。必须记录：影响后续判断的材料与核心结论、事实边界、业务假设与理由、关键决策、资产标识与名称、里程碑、理想态、验证证据摘要、U 行为的分阶段结论。**禁止**记录：密钥、Cookie、临时上传地址、原始敏感日志、供应商完整输出、本机路径、整篇正文。数量必须精确，缺口如实写。

**阶段出口落账。** 每个阶段出口条件全部满足后写一条 `stage_exit` 检查点，内容是出口条件逐项状态与证据指针，只写指针。续作时先找最新 `stage_exit` 定位流程位置，再拉表快照定位内容进度，再按 SKILL.md 路由进入阶段并完整读取阶段文件。恢复后先反读工单里记录的资产当前状态，**禁止**沿用旧记录当现状。从最早未满足的必要条件进入，**禁止**机械从 G1 重做。

## 2. 两类知识、两类问题

| 类别 | 内容 | 绑定到 | 不进入 |
|---|---|---|---|
| 领域知识 | 业务规则、案例、来源、适用范围、生效时间 | 运行时真正需要它的执行者：apc_skill、场景 Skill 文件、FastAgent 提示词、工具集使用指南。能力容器里的文件不天然对 FastAgent 可见，方法见 P3 知识写作一节 | 平台文档 |
| 开发知识 | 工具接入、契约坑、SDK 与部署故障、可复用修复 | 平台与版本；同行业经验库；开发知识库 | 终端用户的业务资料 |
| 业务应用问题 | 表现、输入、产物、版本、根因、改进目标 | 路由到 G1 至 G7 的相应阶段，方法见 P8 | 无 |
| 平台与 Skill 问题 | 可复现证据、受影响版本、临时处置、修复状态 | 统一问题台账。流程固定：收集、分类、跟进、验证、回流。状态固定：待整理、已确认、处理中、待验证、已解决。临时绕过**不算**解决 | 每个应用的私有补丁 |

同行业经验库只收三类条目：业态知识、关键问题、参考经验。每条带适用条件与时间。个别用户的选择**不当作**业态通用规则。展示时按当前场景筛选。过期条目标"待复核"。G1 提问前、G3 识别决策前必须先查。

## 3. 优化能力与"优化已经有效"分开

开发期必须设计并验证六件事：反馈来自哪里；关联哪份输入、产物、版本；修改是纠错还是偏好；适用范围与撤销；下一次哪个节点使用；如何做可比对照。公共策略、模型、工具、编排的变化属于开发变更，进入相应阶段并回归。用户明确的长期偏好属于应用数据行为，在授权范围内生效。单次修改**不得**自动升级为长期偏好。长期收益必须由上线后的实际样本证明。

## 4. 模板选用

脚手架**不内置**多套业务模板。粗浅业务由 AI 直接写出；复杂专业业务我们不具备业务能力。业务应用是把客户方专业知识与 AI 结合的框架。脚手架 demo 只用于理解协议，规则见协议一 A10。产品经理式改进走 P8 的提案制：Agent 提出改进提案，开发者审核，修改后再审核发布。**禁止**绕过开发者改线上。

## 5. 平台适配

**命名口径。** 产品叫业务应用助手。插件、市场、仓库名 `business-app-creator`；MCP 名 `business-app-creator-mcp`；本 Skill 名 `business-app-creator`。交付给用户的是业务应用。场景包只是它的能力容器在平台里的名字，只在工具调用和资产关系里出现；"场景包助手"是旧称。任务点即 TPE，分两型：普通 TPE 派给 TaskAgent 临场执行；编排型 TPE 即 O-TPE，是确定性脚本，编排工具与 FastAgent。"Workflow"是编排型 TPE 的旧称。MCP 工具名 `otpe_manage` 与 `otpe_tool_test`，诊断文档 topic 以 `otpe_` 开头。平台契约字段**不改名**，包括 `tpe_type=workflow`、`workflow_orchestration`、`workflow_runtime_id`。

**三个不能混的概念。** 有业务路线的能力容器才能有业务应用，每个入口是一条路线。一个 `business_ui` 恰好挂一个能力容器，至多一份数据模板，契约全部来自它。业务应用**不是**能力容器。

**平台对象与执行形态。** 正本见 `references/平台对象与运行模型.md`；标识与版本术语见 `references/工具标识与版本术语速查.md`；版本体系见 `references/场景包版本体系与发布逻辑.md`；依赖接入见 `references/依赖与MCP接入.md`。平台运行契约只通过实时 Schema 与 `get_diagnosis_doc` 获取。

**脚手架正本。** 根 `README.md`；`frontend/README.md` 与 `frontend/CLAUDE.md`；`frontend/docs/business-ui-guide.md`；`frontend/schema/README.md` 契约区 v4；`frontend/schema/api/` 公开接口 OpenAPI，一个接口一个文件；`frontend/src/sdk/docs/`；`backend/README.md`；`frontend/docs/dev-host-testing.md`；`frontend/docs/message-guide.md`。官方前端 guidance 副本在 `references/前端设计指南/`。

**手工版与平台版。** 案例库、步级记分、判定器校准、裸跑探针、差分归因、人工任务、回退链、确认即标注、再分配建议、问题台账、可视化工作台，首发一律按手工版执行：结果检查表加列、开发 Agent 手工填表、FastAgent 评分加开发者校准、脚本分支、中途表单收最终用户结果、工单评论加 OA。平台机制建成后替换。本 Skill **不承诺**平台没有的能力。
