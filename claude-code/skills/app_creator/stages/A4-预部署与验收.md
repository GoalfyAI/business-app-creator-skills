# A4 预部署与验收（骨架 · 待预部署工具上线后成文）

> 依赖预部署 / P2S 部署工具，当前开发中。工具上线前，本阶段的替代做法：dev-host 连真后端 + 真模板 schema 的本地全流程验证，并在工单如实记录"未经预部署环境验证"的盲区。

## 骨架（正文待写）

1. 预部署：copy 模板 schema 建临时数据集 → 注入连接（K8s secret）→ P2S 部署；
2. 代理连接下真跑四类页面主线：发起 → 生产 → 回流 → 编辑 → 看板刷新，全链路走一遍；
3. 按《应用验收检查清单》逐条执行（逐条给判定与依据）；
4. 结果检查表用预部署环境的真实数据闭合（mock 不足以闭合）。

## 出口条件（骨架）

- 验收清单全部 通过/不适用；结果检查表全绿；SaaS 链路闭环在真环境走通一次；落 `stage_exit`。
